/**
 * Sales module — public API
 * Streams: Acquire (Lead/Enquiry capture + routing) + Convert (Project pipeline)
 */
export type { Lead, Project, ProjectStakeholder, PipelineSegment } from './domain/types';
